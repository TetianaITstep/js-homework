let productName = "Generator";
let productPrice = 1000;
console.log(
  `Selected "${productName}", price per unit is ${productPrice} credits`
);

productPrice = 2000;
console.log(
  `Selected "${productName}", price per unit is ${productPrice} credits`
);
